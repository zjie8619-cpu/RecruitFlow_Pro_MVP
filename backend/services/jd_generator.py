# backend/services/jd_generator.py
"""
# JD 生成器模块
# 用于生成岗位 JD 的提示词和逻辑
"""
def generate_jd_prompt(title: str, required: str = "", plus: str = "", exclude: str = "") -> str:
"""
#     生成 JD 的提示词
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的提示词字符串
"""
prompt = f"""
# 你是"竞赛线上教育行业 + 互联网教育 + 产品技术 + 销售增长 + 教研命题"多领域的企业级岗位建模专家.
# 你的任务:根据输入信息生成行业级、结构化、可直接用于招聘的 JD.
--------------------------------------------
# [输入信息]
# 岗位名称:{title}
# 必备项:{required}
# 加分项:{plus}
# 排除项:{exclude}
--------------------------------------------
# [步骤 1:自动识别岗位类型(必须执行)]
# 请从以下类型中选择最匹配的一项:
# A. 竞赛类教学岗(数学/物理/化学/生物/信息学竞赛教练)  
# B. 教研与产研岗(讲义、命题、体系、课程设计)  
# C. 技术岗(前端、后端、AI、数据分析)  
# D. 产品岗(教务系统、题库系统、学习系统)  
# E. 运营岗(社群、增长、短视频、自媒体、内容)  
# F. 销售/招生岗(课程顾问、规划师、转化讲师)  
# G. 职能岗(HR、行政、财务、助理)  
# 你必须从以上类别中自动选择一个,并按该岗位类型的行业规范生成 JD.
--------------------------------------------
# [步骤 2:强制遵循 JD 固定模板(必须完全按以下结构输出)]
# 请严格按以下格式生成 JD,每个 Section 必须空一行,列表必须独立成行,不得合并到同一行:
# [岗位使命]
# (请生成岗位使命内容)
# [岗位职责]
# (请输出 1)xxx
2)xxx
# 3)xxx 这样的格式,每条独立一行)
# [任职要求]
# 必备:
# (请逐条列出 {required},保持 1)格式)
# 加分:
# (逐条列出 {plus})
# 排除项(不考虑):
# (逐条列出 {exclude})
# [KPI/关键结果]
# (每条独立一行,使用 1)格式)
# [工作方式/地点]
# 请生成合理内容
# [薪资范围]
# 请生成 xx-xxK·13薪 或 面议
# [福利亮点]
# (逐条列出)
# [面试流程]
# (必须分行列出,例如:
# 1)简历筛选
# 2)初面
# 3)复面
# 4)发 Offer)
--------------------------------------------
# [步骤 3:行业专业化要求(严禁忽略)]
# A. 若为竞赛岗:  
# - 必须写竞赛体系(如 NOIBC/IBO、五大学科奥赛)  
# - 必须写训练模块(遗传/细胞/分子/生态…)  
# - 必须写命题能力、讲义制作、刷题体系  
# - 必须写学生成绩目标  
# B. 若为教研岗:  
# - 必须写讲义、题库、命题、课程体系  
# C. 若为技术岗:  
# - 必须写技术栈、工程能力、系统设计  
# D. 若为运营岗:  
# - 必须写流量、增长、内容、数据指标  
# E. 若为销售岗:  
# - 必须写线索转化、跟进流程、目标指标  
# F. 若为职能岗:  
# - 必须写流程建设、制度执行  
--------------------------------------------
# [步骤 4:输出要求]
# - 格式必须精准按模板输出  
# - 内容必须行业真实、专业  
# - 不得输出任何解释、分析、思考过程  
# - 只能输出最终 JD 文本  
"""
return prompt
def generate_jd(title: str, required: str = "", plus: str = "", exclude: str = ""):
"""
#     生成 JD
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的 JD 提示词(通过调用 generate_jd_prompt)
"""
return generate_jd_prompt(title=title, required=required, plus=plus, exclude=exclude)
# JD 生成器模块
# 用于生成岗位 JD 的提示词和逻辑
"""
def generate_jd_prompt(title: str, required: str = "", plus: str = "", exclude: str = "") -> str:
"""
#     生成 JD 的提示词
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的提示词字符串
"""
prompt = f"""
# 你是"竞赛线上教育行业 + 互联网教育 + 产品技术 + 销售增长 + 教研命题"多领域的企业级岗位建模专家.
# 你的任务:根据输入信息生成行业级、结构化、可直接用于招聘的 JD.
--------------------------------------------
# [输入信息]
# 岗位名称:{title}
# 必备项:{required}
# 加分项:{plus}
# 排除项:{exclude}
--------------------------------------------
# [步骤 1:自动识别岗位类型(必须执行)]
# 请从以下类型中选择最匹配的一项:
# A. 竞赛类教学岗(数学/物理/化学/生物/信息学竞赛教练)  
# B. 教研与产研岗(讲义、命题、体系、课程设计)  
# C. 技术岗(前端、后端、AI、数据分析)  
# D. 产品岗(教务系统、题库系统、学习系统)  
# E. 运营岗(社群、增长、短视频、自媒体、内容)  
# F. 销售/招生岗(课程顾问、规划师、转化讲师)  
# G. 职能岗(HR、行政、财务、助理)  
# 你必须从以上类别中自动选择一个,并按该岗位类型的行业规范生成 JD.
--------------------------------------------
# [步骤 2:强制遵循 JD 固定模板(必须完全按以下结构输出)]
# 请严格按以下格式生成 JD,每个 Section 必须空一行,列表必须独立成行,不得合并到同一行:
# [岗位使命]
# (请生成岗位使命内容)
# [岗位职责]
# (请输出 1)xxx
2)xxx
# 3)xxx 这样的格式,每条独立一行)
# [任职要求]
# 必备:
# (请逐条列出 {required},保持 1)格式)
# 加分:
# (逐条列出 {plus})
# 排除项(不考虑):
# (逐条列出 {exclude})
# [KPI/关键结果]
# (每条独立一行,使用 1)格式)
# [工作方式/地点]
# 请生成合理内容
# [薪资范围]
# 请生成 xx-xxK·13薪 或 面议
# [福利亮点]
# (逐条列出)
# [面试流程]
# (必须分行列出,例如:
# 1)简历筛选
# 2)初面
# 3)复面
# 4)发 Offer)
--------------------------------------------
# [步骤 3:行业专业化要求(严禁忽略)]
# A. 若为竞赛岗:  
# - 必须写竞赛体系(如 NOIBC/IBO、五大学科奥赛)  
# - 必须写训练模块(遗传/细胞/分子/生态…)  
# - 必须写命题能力、讲义制作、刷题体系  
# - 必须写学生成绩目标  
# B. 若为教研岗:  
# - 必须写讲义、题库、命题、课程体系  
# C. 若为技术岗:  
# - 必须写技术栈、工程能力、系统设计  
# D. 若为运营岗:  
# - 必须写流量、增长、内容、数据指标  
# E. 若为销售岗:  
# - 必须写线索转化、跟进流程、目标指标  
# F. 若为职能岗:  
# - 必须写流程建设、制度执行  
--------------------------------------------
# [步骤 4:输出要求]
# - 格式必须精准按模板输出  
# - 内容必须行业真实、专业  
# - 不得输出任何解释、分析、思考过程  
# - 只能输出最终 JD 文本  
"""
return prompt
def generate_jd(title: str, required: str = "", plus: str = "", exclude: str = ""):
"""
#     生成 JD
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的 JD 提示词(通过调用 generate_jd_prompt)
"""
return generate_jd_prompt(title=title, required=required, plus=plus, exclude=exclude)
# JD 生成器模块
# 用于生成岗位 JD 的提示词和逻辑
"""
def generate_jd_prompt(title: str, required: str = "", plus: str = "", exclude: str = "") -> str:
"""
#     生成 JD 的提示词
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的提示词字符串
"""
prompt = f"""
# 你是"竞赛线上教育行业 + 互联网教育 + 产品技术 + 销售增长 + 教研命题"多领域的企业级岗位建模专家.
# 你的任务:根据输入信息生成行业级、结构化、可直接用于招聘的 JD.
--------------------------------------------
# [输入信息]
# 岗位名称:{title}
# 必备项:{required}
# 加分项:{plus}
# 排除项:{exclude}
--------------------------------------------
# [步骤 1:自动识别岗位类型(必须执行)]
# 请从以下类型中选择最匹配的一项:
# A. 竞赛类教学岗(数学/物理/化学/生物/信息学竞赛教练)  
# B. 教研与产研岗(讲义、命题、体系、课程设计)  
# C. 技术岗(前端、后端、AI、数据分析)  
# D. 产品岗(教务系统、题库系统、学习系统)  
# E. 运营岗(社群、增长、短视频、自媒体、内容)  
# F. 销售/招生岗(课程顾问、规划师、转化讲师)  
# G. 职能岗(HR、行政、财务、助理)  
# 你必须从以上类别中自动选择一个,并按该岗位类型的行业规范生成 JD.
--------------------------------------------
# [步骤 2:强制遵循 JD 固定模板(必须完全按以下结构输出)]
# 请严格按以下格式生成 JD,每个 Section 必须空一行,列表必须独立成行,不得合并到同一行:
# [岗位使命]
# (请生成岗位使命内容)
# [岗位职责]
# (请输出 1)xxx
2)xxx
# 3)xxx 这样的格式,每条独立一行)
# [任职要求]
# 必备:
# (请逐条列出 {required},保持 1)格式)
# 加分:
# (逐条列出 {plus})
# 排除项(不考虑):
# (逐条列出 {exclude})
# [KPI/关键结果]
# (每条独立一行,使用 1)格式)
# [工作方式/地点]
# 请生成合理内容
# [薪资范围]
# 请生成 xx-xxK·13薪 或 面议
# [福利亮点]
# (逐条列出)
# [面试流程]
# (必须分行列出,例如:
# 1)简历筛选
# 2)初面
# 3)复面
# 4)发 Offer)
--------------------------------------------
# [步骤 3:行业专业化要求(严禁忽略)]
# A. 若为竞赛岗:  
# - 必须写竞赛体系(如 NOIBC/IBO、五大学科奥赛)  
# - 必须写训练模块(遗传/细胞/分子/生态…)  
# - 必须写命题能力、讲义制作、刷题体系  
# - 必须写学生成绩目标  
# B. 若为教研岗:  
# - 必须写讲义、题库、命题、课程体系  
# C. 若为技术岗:  
# - 必须写技术栈、工程能力、系统设计  
# D. 若为运营岗:  
# - 必须写流量、增长、内容、数据指标  
# E. 若为销售岗:  
# - 必须写线索转化、跟进流程、目标指标  
# F. 若为职能岗:  
# - 必须写流程建设、制度执行  
--------------------------------------------
# [步骤 4:输出要求]
# - 格式必须精准按模板输出  
# - 内容必须行业真实、专业  
# - 不得输出任何解释、分析、思考过程  
# - 只能输出最终 JD 文本  
"""
return prompt
def generate_jd(title: str, required: str = "", plus: str = "", exclude: str = ""):
"""
#     生成 JD
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的 JD 提示词(通过调用 generate_jd_prompt)
"""
return generate_jd_prompt(title=title, required=required, plus=plus, exclude=exclude)
# JD 生成器模块
# 用于生成岗位 JD 的提示词和逻辑
"""
def generate_jd_prompt(title: str, required: str = "", plus: str = "", exclude: str = "") -> str:
"""
#     生成 JD 的提示词
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的提示词字符串
"""
prompt = f"""
# 你是"竞赛线上教育行业 + 互联网教育 + 产品技术 + 销售增长 + 教研命题"多领域的企业级岗位建模专家.
# 你的任务:根据输入信息生成行业级、结构化、可直接用于招聘的 JD.
--------------------------------------------
# [输入信息]
# 岗位名称:{title}
# 必备项:{required}
# 加分项:{plus}
# 排除项:{exclude}
--------------------------------------------
# [步骤 1:自动识别岗位类型(必须执行)]
# 请从以下类型中选择最匹配的一项:
# A. 竞赛类教学岗(数学/物理/化学/生物/信息学竞赛教练)  
# B. 教研与产研岗(讲义、命题、体系、课程设计)  
# C. 技术岗(前端、后端、AI、数据分析)  
# D. 产品岗(教务系统、题库系统、学习系统)  
# E. 运营岗(社群、增长、短视频、自媒体、内容)  
# F. 销售/招生岗(课程顾问、规划师、转化讲师)  
# G. 职能岗(HR、行政、财务、助理)  
# 你必须从以上类别中自动选择一个,并按该岗位类型的行业规范生成 JD.
--------------------------------------------
# [步骤 2:强制遵循 JD 固定模板(必须完全按以下结构输出)]
# 请严格按以下格式生成 JD,每个 Section 必须空一行,列表必须独立成行,不得合并到同一行:
# [岗位使命]
# (请生成岗位使命内容)
# [岗位职责]
# (请输出 1)xxx
2)xxx
# 3)xxx 这样的格式,每条独立一行)
# [任职要求]
# 必备:
# (请逐条列出 {required},保持 1)格式)
# 加分:
# (逐条列出 {plus})
# 排除项(不考虑):
# (逐条列出 {exclude})
# [KPI/关键结果]
# (每条独立一行,使用 1)格式)
# [工作方式/地点]
# 请生成合理内容
# [薪资范围]
# 请生成 xx-xxK·13薪 或 面议
# [福利亮点]
# (逐条列出)
# [面试流程]
# (必须分行列出,例如:
# 1)简历筛选
# 2)初面
# 3)复面
# 4)发 Offer)
--------------------------------------------
# [步骤 3:行业专业化要求(严禁忽略)]
# A. 若为竞赛岗:  
# - 必须写竞赛体系(如 NOIBC/IBO、五大学科奥赛)  
# - 必须写训练模块(遗传/细胞/分子/生态…)  
# - 必须写命题能力、讲义制作、刷题体系  
# - 必须写学生成绩目标  
# B. 若为教研岗:  
# - 必须写讲义、题库、命题、课程体系  
# C. 若为技术岗:  
# - 必须写技术栈、工程能力、系统设计  
# D. 若为运营岗:  
# - 必须写流量、增长、内容、数据指标  
# E. 若为销售岗:  
# - 必须写线索转化、跟进流程、目标指标  
# F. 若为职能岗:  
# - 必须写流程建设、制度执行  
--------------------------------------------
# [步骤 4:输出要求]
# - 格式必须精准按模板输出  
# - 内容必须行业真实、专业  
# - 不得输出任何解释、分析、思考过程  
# - 只能输出最终 JD 文本  
"""
return prompt
def generate_jd(title: str, required: str = "", plus: str = "", exclude: str = ""):
"""
#     生成 JD
#     参数:
#         title: 岗位名称
#         required: 必备技能
#         plus: 加分项
#         exclude: 排除项
#     返回:
#         生成的 JD 提示词(通过调用 generate_jd_prompt)
"""
return generate_jd_prompt(title=title, required=required, plus=plus, exclude=exclude)
